{
  "name": "@dwell/core",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "corpus"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:imports": "node scripts/check-imports.mjs"
  },
  "devDependencies": {
    "vitest": "^3.2.0",
    "typescript": "^5.5.0"
  }
}
